package main

import "fmt"

func constants() {
	//constants are used declare constants if any, they can be declared anywhere where variables can be declared
	const n = 9
	const a = "chara"
	fmt.Print(n, a)
}
